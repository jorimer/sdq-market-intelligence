"""Gate E — trade-resilience backtest (regional panel).

Validates the resilience score (export concentration + import dependency) against
realized external shocks across a broad LatAm+Caribbean panel, mirroring the IRMP
methodology backtest. Product stays RD-only; this peer set is validation-only and
gives the backtest statistical power (more cross-sectional units → tighter CI).
"""
